# On Desktop with Internet

make dependencies folder
pip download -r requirements.txt -d "./dependencies"
zip the dependencies folder and pythoninstaller exe  and send to machine without internet

# On Desktop without Internet

install python using exe
create venv using pip -m venv venv
activate venv using ./venv/scripts/activate

unzip the dependencies folder
cd dependencies
for %x in (dir *.whl) do python -m pip install %x

edit config.json file and update folder path (ensure no special characters in the folder name)
write in this format for windows "C:\\Users\\Pratham\\BARC\\camera-calibration\\images" 
also add chessboard size 

create "images" folder

run mainapp.py

go through calibration wizard

calibration pkl and extrinsicParams pkl will be generated

now you can run the ml model 